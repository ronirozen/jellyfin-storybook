import { ListItemIcon as ListItemIconMui } from "@mui/material";

import { ListItemIconProps } from "./ListItemIcon.type";

export const ListItemIcon = ({ ...props }: ListItemIconProps) => <ListItemIconMui {...props} />;
